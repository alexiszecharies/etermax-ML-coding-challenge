Esta solucion se basa en 3 notebooks. 

El primero es 1. Prueba ML Etermax donde esta codigo del desarrollo del modelo y guarda en la carpeta model los modelos y algunos datos que se necesitan en el siguiente notebook para trabajar los datos.

El segundo es 2. Flask_server donde se levanta flask y se corre para que quede esperando un post con los datos a predecir revenue.

El tercero es 3. Test request donde hay un request para probar la API y se muestra que el resultado es el esperado.